# --- Data acquisition ---
yfinance>=0.2.40        # daily OHLCV futures data (gold, silver, WTI crude)
fredapi>=0.5.1          # optional macro/fundamental context (FRED)
requests>=2.31          # optional EIA API calls

# --- Core data handling ---
pandas>=2.0
numpy>=1.24

# --- Anomaly / changepoint detection ---
scikit-learn>=1.3       # Isolation Forest, preprocessing
ruptures>=1.1.9         # Bayesian/offline changepoint detection

# --- Forecasting baseline ---
statsmodels>=0.14       # ARIMA baseline
# prophet>=1.1.5         # optional alternative — heavier install (needs cmdstanpy), only add if you want it

# --- Backtesting ---
# backtesting.py>=0.3.3  # optional — only if you want a proper backtest engine rather than a vectorised pandas one

# --- Visualisation & dashboard ---
matplotlib>=3.7
plotly>=5.20
streamlit>=1.35
